export class DriverModel {
    constructor(public firstName: string, public lastName: string, public status: string,
        public mobileNo: number, public email: string, public driverId: string, public sex: string) { }
}
